The scripts here will be executed before every build. If you have scripts that
should only be executed _once_ before the initial setup of the sdk, they should
go in `hooks/prepare_{sdk,system}` instead.
